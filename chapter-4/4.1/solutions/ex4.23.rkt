#lang sicp

;; the version in text will produce a nested lambda expression, while the
;; version that the Alyssa provide will produce a normal lambda body (like 
;; representing in a begin expression), while the body expression needs to 
;; be analyzed when it is executed, which taking more time.
